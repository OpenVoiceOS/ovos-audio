name: Run License Tests
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - dev
  workflow_dispatch:

jobs:
  license_tests:
    uses: TigreGotico/gh-automations/.github/workflows/license-check.yml@master
    with:
      system_deps: 'swig'
